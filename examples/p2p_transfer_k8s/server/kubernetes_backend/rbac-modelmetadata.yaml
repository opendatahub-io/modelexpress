# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# RBAC resources for ModelExpress CRD-based metadata
# Required when using MX_METADATA_BACKEND=kubernetes
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: modelexpress
  labels:
    app.kubernetes.io/name: modelexpress
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: modelexpress-metadata
  labels:
    app.kubernetes.io/name: modelexpress
rules:
  # ModelMetadata CRD access
  - apiGroups:
      - modelexpress.nvidia.com
    resources:
      - modelmetadatas
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  # ModelMetadata status subresource
  - apiGroups:
      - modelexpress.nvidia.com
    resources:
      - modelmetadatas/status
    verbs:
      - get
      - update
      - patch
  # ConfigMaps for tensor descriptors.
  # NOTE: This grants access to ALL ConfigMaps in the namespace. K8s RBAC does not
  # support label-based resource filtering. In shared namespaces, consider using a
  # dedicated namespace for ModelExpress workloads.
  - apiGroups:
      - ""
    resources:
      - configmaps
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  # ModelCacheEntry CRD for the registry backend (same MX_METADATA_BACKEND value).
  - apiGroups:
      - modelexpress.nvidia.com
    resources:
      - modelcacheentries
    verbs:
      - get
      - list
      - create
      - update
      - patch
      - delete
  # ModelCacheEntry status subresource
  - apiGroups:
      - modelexpress.nvidia.com
    resources:
      - modelcacheentries/status
    verbs:
      - get
      - update
      - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: modelexpress-metadata
  labels:
    app.kubernetes.io/name: modelexpress
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: modelexpress-metadata
subjects:
  # subjects[].namespace omitted on purpose: for a namespaced RoleBinding,
  # a ServiceAccount subject without an explicit namespace resolves to the
  # RoleBinding's own namespace (whatever `kubectl apply -n <ns>` lands in).
  - kind: ServiceAccount
    name: modelexpress
